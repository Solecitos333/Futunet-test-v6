## 2024-05-01 - [Add aria-labels to icon-only buttons]
**Learning:** Dynamically generated HTML components in JavaScript (e.g., cart and product quantity controls using template literals) lack static analysis coverage for accessibility.
**Action:** Ensure `aria-label` attributes are explicitly added to icon-only buttons like `-` and `+` during dynamic generation.
